// Rotate a matrix 90 degrees

function rotateMatrix (matrix) {
    const len = matrix.length;
    const out = [];
    let i = 0;
    while (i < len) {
        j = len - 1;
        while (j >= 0) {
            
        }
    }
}